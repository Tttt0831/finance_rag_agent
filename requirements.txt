# 核心框架
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0   # HuggingFaceEmbeddings（替代已弃用的 community 版）

# Agent 框架（create_react_agent / MemorySaver / SqliteSaver）
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0

# 混合检索（BM25 + RRF）
rank_bm25>=0.2.2
jieba>=0.42.1

# 向量数据库
chromadb>=0.5.0

# 本地嵌入模型（无需 API Key）
sentence-transformers>=3.0.0

# 数据处理
pandas>=2.2.0
numpy>=1.26.0

# 文档加载
pypdf>=4.0.0
openpyxl>=3.1.0          # Excel 文档支持

# Web 服务
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9  # 文件上传支持

# 前端界面
gradio>=4.0.0

# 工具库
python-dotenv>=1.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
requests>=2.32.0
